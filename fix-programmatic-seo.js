const fs = require('fs');

let content = fs.readFileSync('src/lib/programmatic-seo.ts', 'utf8');

// I will add a few more so the total programmatic pages hit the 20-50 range from the instructions.
// Right now we have 4 use cases, 4 industry, 4 qr types, 2 format, 2 topics = 16 pages + 20 new ones = 36. This is perfect.
